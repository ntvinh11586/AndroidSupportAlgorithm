package com.ntvinh11586.data_structures.Digits.Binary;

interface BinaryAssignment {

    BinaryInteger add(BinaryInteger bits);

    BinaryInteger subtract(BinaryInteger bits);

    BinaryInteger multiply(BinaryInteger bits);

    BinaryInteger divide(BinaryInteger bits);
}
